version: '3.8'

services:
  # Frontend service (nginx serving static files with SSL support)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
    volumes:
      # SSL certificates
      - ./ssl/certbot/conf:/etc/letsencrypt
      - ./ssl/certbot/www:/var/www/certbot
    restart: unless-stopped
    networks:
      - gmat-quiz-network

  # Certbot for SSL certificate management
  certbot:
    image: certbot/certbot
    restart: unless-stopped
    volumes:
      - ./ssl/certbot/conf:/etc/letsencrypt
      - ./ssl/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - gmat-quiz-network

  # Backend service (FastAPI on port 8000)
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      # Mount the database and user data directories to persist data
      - ./quiz.db:/app/quiz.db
      - ./user_databases:/app/user_databases
      - ./JSON:/app/JSON
      - ./images:/app/images
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - ALLOW_USER_REGISTRATION=${ALLOW_USER_REGISTRATION:-true}
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import requests; requests.get(\"http://localhost:8000/health\")' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - gmat-quiz-network

networks:
  gmat-quiz-network:
    driver: bridge
